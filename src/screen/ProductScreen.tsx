import React from "react";
import { View, Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export function ProductScreen() {
  return (
    <SafeAreaView className="flex-1">
      <Text>
        Only to show that i am navigating to this screen just to let you know
        that i am understanding the topic of navigation. As per the
        requirements, I have created this screen to show navigation.
      </Text>
    </SafeAreaView>
  );
}
